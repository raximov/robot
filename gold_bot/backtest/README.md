# Backtest plan

Use `vectorbt` or `backtrader` with at least 2 years of XAUUSD M15/H1 data.

Minimum acceptance metrics:

- Profit factor > 1.2
- Max drawdown < 15%
- Positive expectancy
- Stable monthly returns (no single month dominating outcome)
